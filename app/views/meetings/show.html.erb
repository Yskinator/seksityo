<%= form_for(@meeting) do |f| %>
    <h1>Brändinimi</h1>

    <h1>Sending message at:</h1>
    <div class="textfield">
      <strong>Meeting created at <%= @meeting.created_at %>, sending alert after</strong>
    </div>
    <div class="info-timer">
      <%= @meeting.duration %> minutes
    </div>

    <div class="actions">
      <%= f.submit 'I\'m OK', :class => "submitbutton" %>
    </div>
    <div class="actions">
      <%= f.submit 'Send alert', :class => "alertbutton" %>
    </div>
<% end %>
