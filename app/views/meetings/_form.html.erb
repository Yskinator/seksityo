<%= form_for(@meeting) do |f| %>
    <div class="field-section color-1">
      <div class="field-explanation">
        <h2>Who are you?</h2>
        <p>We need to inform your Artemis someone is in trouble. But who?</p>
      </div>
      <div class="field">
        <%= f.text_field :nickname, :class => "textfield", placeholder: "My name" %>
      </div>
    </div>
    <div class="field-section color-2">
      <div class="field-explanation">
        <h2>Who is your Artemis?</h2>
        <p>We need to contact your Artemis in case we don't hear from you. <br/>
          Enter his/her email here.</p>
      </div>
      <div class="field">
        <%= f.text_field :phone_number, :class => "textfield", placeholder: "Phone number" %>
        <% if @meeting.errors.messages[:phone_number].any? %>
            <div class="error-explanation">
              <p class="error-message">Phone number is invalid</p>
            </div>
        <% end %>
      </div>
    </div>
    <div class="field-section color-3">
      <div class="field-explanation">
        <h2>How long should we wait?</h2>
        <p>Your artemis should be worried if you don't tell us you're safe by..</p>
      </div>
      <div class="field">
        <div class="duration-container">
          <a id="select30m" class="duration-select" data-turbolinks="false">30m</a>
        </div>
        <div class="duration-container">
          <a id="select1h" class="duration-select" data-turbolinks="false">1h</a>
        </div>
        <div class="duration-container">
          <a id="select2h" class="duration-select" data-turbolinks="false">2h</a>
        </div>
        <div id="duration-value">
        </div>
        <div class="duration-container">
          <input class="duration-input" type="number" id="duration-input" min="0" max="1440" placeholder="Minutes">
        </div>
      </div>
    </div>
    <div class="field-section color-4">
      <div class="field-explanation">
        <h2>Ready to go?</h2>
        <p>Once you press Start Timer, we start the clock and inform your Artemis if we don't hear from you in <span id="duration-info"></span> minutes</p>
      </div>
        <div class="actions">
          <%= f.submit 'Start timer', :class => "submitbutton" %>
        </div>

    </div>

<% end %>
